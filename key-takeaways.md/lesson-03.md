# Câu lệnh git sửa commit gần nhất, bỏ file khỏi staging area, back lại commit trước đó và giữ nguyên code đang sửa hiện tại

git commit --amend
git commit --amend -m "message"
git restore --staged <file>
git reset HEAD~1

# Javascript

- Object với const
- Thêm sửa xóa với object
- Loop
- Function
